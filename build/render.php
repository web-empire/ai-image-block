<?php
/**
 * PHP file to use when rendering the block type on the server to show on the front end.
 *
 * The following variables are exposed to the file:
 *     $attributes (array): The block attributes.
 *     $content (string): The block default content.
 *     $block (WP_Block): The block instance.
 *
 * @see https://github.com/WordPress/gutenberg/blob/trunk/docs/reference-guides/block-api/block-metadata.md#render
 * @package block-development-examples
 */

if ( ! defined( 'ABSPATH' ) ) {
	exit; // Exit if accessed directly.
}

$image_url = $attributes['imgUrl'] ?? '';
$prompt = $attributes['prompt'] ?? __( 'Image generated by AI Image Block', 'ai-image-block' );

if ( empty( $image_url ) ) {
	$image_url = AI_IMAGE_BLOCK_URL . 'assets/placeholder.jpg';
}

?>

<img src="<?php echo esc_url( $image_url ); ?>" alt="<?php echo esc_attr( $prompt ); ?>" />
